
<%= form_for(workout, html: {class: "form-horizontal"}) do |f| %>

    <div class="form-group">
        <%= f.label :name, 'Name:', class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.text_field :name, class: 'form-control' %><br>
        </div>
    </div>

    <div class="form-group">
        <%= f.label :category, 'Select all categories that apply', class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.collection_check_boxes :category_ids, Category.all, :id, :name, class: 'form-control' %>
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <div class="checkbox">
                <%= f.check_box :is_private, {checked: true} %>
            <%= f.label :is_private, 'Private' %>
        </div>
        </div>
    </div>
    <h4>Exercises</h2>

    <%= f.fields_for :exercises do |builder| %>
        <%= render partial: 'exercise_fields', locals: {s: builder} %>
    <% end %>

    <%= f.submit %>
<% end %>
